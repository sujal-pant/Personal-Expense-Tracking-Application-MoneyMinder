@inherits LayoutComponentBase

<div class="page">
    @if (!NavigationManager.Uri.Contains("login", StringComparison.OrdinalIgnoreCase))
    {
        <div class="sidebar">
            <NavMenu />
        </div>
    }

    <main class="@(!NavigationManager.Uri.Contains("login", StringComparison.OrdinalIgnoreCase) ? "main-content" : "full-page")">
        <div class="top-row px-4">
            <a href="https://learn.microsoft.com/aspnet/core/" target="_blank">About</a>
        </div>

        <article class="content px-4">
            @Body
        </article>
    </main>
</div>

@* <style>
     .page {
         display: flex;
         height: 100vh;
     }

     .sidebar {
         width: 200px;
         background: #002b49;
         color: white;
     }

     .main-content {
         flex: 1;
         margin-left: 200px;
         display: flex;
         flex-direction: column;
     }

     .full-page {
         flex: 1;
         margin: 0;
         display: flex;
         flex-direction: column;
     }

     .top-row {
         background-color: #f5f5f5;
         padding: 1rem;
         border-bottom: 1px solid #ddd;
     }

     .content {
         flex: 1;
         padding: 2rem;
         overflow-y: auto;
     }
 </style> *@

@code {
    [Inject]
    private NavigationManager NavigationManager { get; set; }
}
